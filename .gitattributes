# Auto-detect text files and normalize to LF in the repo
* text=auto

# Force LF for common source files
*.py text eol=lf
*.sh text eol=lf
*.md text eol=lf
*.yml text eol=lf
*.yaml text eol=lf

# Keep Jupyter notebooks as binary (avoid spurious diffs)
*.ipynb binary

# Ensure Windows-specific files are treated as binary
*.exe binary
*.dll binary

# Ignore large or generated DB files
*.sqlite3 binary
